import { defineStyleConfig } from '@chakra-ui/react'

export const Alert = defineStyleConfig({
  baseStyle: {
    title: { color: "white" },
    color: 'white',
    icon: {
      color: 'white',
      path: { fill: "white" },
    },
  },
})
